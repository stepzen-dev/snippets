# maps some of JSON response values to the fields of the GraphQL result.
query CustomerByID($id: ID!) {
  customerByID(id: $id) {
    cAddress {
      city
      state
      country
    }
    cId
    cName
  }
}
