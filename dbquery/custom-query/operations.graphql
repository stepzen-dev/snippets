# Retrieve a specific customer by ID
query Customer {
  customer(id: 1) {
    id
    name
    email
  }
}

# Retrieve all customers in the system
query Customers {
  customers {
    id
    name
    email
  }
}
