query CreateAndSort($count: Int!, $type: String) {
  createAndSort(count: $count, type: $type) {
    docs
  }
}
query reRank($docs: JSON, $type: String) {
  reRank(docs: $docs, type: $type) {
    docs
  }
}
